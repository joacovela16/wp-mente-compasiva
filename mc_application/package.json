{
  "name": "mentecompasiva-theme",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prod": "windicss './**/*.{php,svelte}' -d -to assets/styles/windi.css",
    "watch": "windicss  './**/*.{php,svelte}' '../mc_plugin/**/*.{php,svelte}' -d -to assets/styles/windi.css --config windi.config.js "
  },
  "devDependencies": {
    "windicss": "3.4.2"
  }
}
